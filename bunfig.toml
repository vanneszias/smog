[install]
# Expo native builds require a single installation of every native module.
# A hoisted workspace layout prevents Bun from creating peer-specific copies
# that Expo Doctor (and occasionally native autolinking) treats as duplicates.
linker = "hoisted"
